# Homebrew Tap

macOS and Linux users can install coldstorage with:

```sh
brew install crueber/tap/coldstorage
```

The formula here is regenerated automatically by coldstorage's release
workflow on every `v*` tag — do not edit it by hand except for structural
changes to the formula itself.
